{{- if .Values.vault.enabled }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "basket-api.fullname" . }}-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "basket-api.labels" . | nindent 4 }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "basket-api.fullname" . }}-secrets
    creationPolicy: Owner
    template:
      type: Opaque
      metadata:
        labels:
          {{- include "basket-api.labels" . | nindent 10 }}
  data:
    - secretKey: REDIS_CONNECTION
      remoteRef:
        key: {{ .Values.vault.secretPath }}
        property: REDIS_CONNECTION
    - secretKey: IDENTITY_URL
      remoteRef:
        key: {{ .Values.vault.secretPath }}
        property: IDENTITY_URL
  dataFrom:
    - extract:
        key: secret/eshop/global
{{- end }}
