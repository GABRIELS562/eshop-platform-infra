{{- if .Values.vault.enabled }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "catalog-api.fullname" . }}-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "catalog-api.labels" . | nindent 4 }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "catalog-api.fullname" . }}-secrets
    creationPolicy: Owner
    template:
      type: Opaque
      metadata:
        labels:
          {{- include "catalog-api.labels" . | nindent 10 }}
  data:
    - secretKey: DATABASE_CONNECTION
      remoteRef:
        key: {{ .Values.vault.secretPath }}
        property: DATABASE_CONNECTION
    - secretKey: IDENTITY_URL
      remoteRef:
        key: {{ .Values.vault.secretPath }}
        property: IDENTITY_URL
  dataFrom:
    - extract:
        key: secret/eshop/global
{{- end }}
