---
# ExternalSecret for Basket API service
# Contains Redis connection and Identity service URL
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: basket-api-secrets
  namespace: eshop
  labels:
    app.kubernetes.io/name: basket-api-secrets
    app.kubernetes.io/component: configuration
    app.kubernetes.io/part-of: eshop-platform
    app.kubernetes.io/managed-by: external-secrets
spec:
  # How often to refresh secrets from Vault
  refreshInterval: "1h"
  # Reference to the ClusterSecretStore
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  # Target Kubernetes Secret configuration
  target:
    name: basket-api-secrets
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/name: basket-api-secrets
          app.kubernetes.io/component: configuration
          app.kubernetes.io/part-of: eshop-platform
  # Data mappings from Vault to Kubernetes Secret
  data:
    - secretKey: REDIS_CONNECTION
      remoteRef:
        key: secret/data/eshop/basket-api
        property: REDIS_CONNECTION
    - secretKey: IDENTITY_URL
      remoteRef:
        key: secret/data/eshop/basket-api
        property: IDENTITY_URL
